# BrightSign Deployment Configuration
# Copy this file to .env and update with your player details

# BrightSign Player Configuration
# Uncomment and set your player's IP address
# BRIGHTSIGN_PLAYER_IP=192.168.0.51

# Alternative: Use player hostname if DNS/mDNS is configured
# BRIGHTSIGN_PLAYER_HOST=brightsign-player.local

# Diagnostic Web Server Port (default: 8008)
# BRIGHTSIGN_DWS_PORT=8008

# Optional: Player credentials if authentication is required
# BRIGHTSIGN_USERNAME=admin
# BRIGHTSIGN_PASSWORD=admin

# Development Settings
# Auto-deploy on file change (for watch mode)
# AUTO_DEPLOY_ON_SAVE=false

# Skip reboot after deployment (faster iteration)
# SKIP_REBOOT_AFTER_DEPLOY=false
